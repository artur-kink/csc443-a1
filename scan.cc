#include "library.h"

/**
 * Print out all records in a heapfile.
 */
int main(int argc, char** argv){
    if(argc != 3){
        printf("Usage: scan <heapfile> <page_size>\n");
        return 1;
    }
    
    
    
}
